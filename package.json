{
  "name": "quickbooks-mcp-chat-assistant",
  "version": "1.0.0",
  "description": "A complete QuickBooks MCP Chat Assistant with React frontend and Node.js MCP server",
  "private": true,
  "workspaces": [
    "frontend",
    "mcp-server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:server\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:server": "cd mcp-server && npm run dev",
    "build": "npm run build:frontend && npm run build:server",
    "build:frontend": "cd frontend && npm run build",
    "build:server": "cd mcp-server && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../mcp-server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
